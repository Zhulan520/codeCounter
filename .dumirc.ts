import { defineConfig } from 'dumi';

export default defineConfig({
  outputPath: 'docs-dist',
  title: 'codeCounter',
  base: '/codeCounter/',
  publicPath: '/codeCounter/',
  themeConfig: {
    name: 'codeCounter',
  },
});
